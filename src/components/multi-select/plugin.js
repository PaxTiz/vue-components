import MultiselectComponent from '../multi-select/Multiselect.vue'

const Multiselect = {
    install (Vue) {
        Vue.component('Multiselect', MultiselectComponent)
    }
}

export default Multiselect
